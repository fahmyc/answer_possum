<ul>
	<% if !admin_signed_in? %>
	<li><%= link_to "Home", 		root_path %></li>
	<% end %>

    <% if !student_signed_in? && !tutor_signed_in? && !admin_signed_in? %>
		<li><%= link_to "Admin", 	new_admin_session_path %></li>
    <% end %>

    <% if admin_signed_in? %>
    	<li><%= link_to "Admin Home", admin_show_path %></li>
   	<% end %>


	
</ul>